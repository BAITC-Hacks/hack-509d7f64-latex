# Python 3.12 or 3.13 (espnet does not support 3.14 yet). Install parallel_wavegan without build isolation:
#   pip install -r tts/requirements.txt && pip install --no-build-isolation parallel_wavegan
torch>=2.9
espnet
silero>=0.5.0
silero-stress
numpy
soundfile
pyyaml
fastapi
uvicorn[standard]
